<%= form_for(create_comment) do |f| %>
  <% if create_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(create_comment.errors.count, "error") %> prohibited this create_comment from being saved:</h2>

      <ul>
      <% create_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_field :comment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

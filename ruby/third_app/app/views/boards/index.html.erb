<div class="wrapper wrapper_bg">
<p id="notice"><%= notice %></p>
<div class="wrapper">
<h1>僕はできそこないです</h1>

<div class="menu">
<table>
  <thead>
    <tr>
      <th>題名</th>
      <th>内容</th>
      <th colspan="3"></th>
    </tr>
  </thead>
</div>
<div class="container">
  <tbody>
    <% @boards.each do |board| %>
      <tr>
        <td><%= board.title %></td>
        <td><%= board.editor %></td>
        <td><%= link_to 'Show', board %></td>
        <td><%= link_to 'Edit', edit_board_path(board) %></td>
        <td><%= link_to 'Destroy', board, method: :delete, data: { confirm: '本当にいいのかい？' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
</div>



<%= link_to 'New Board', new_board_path %>
</div>
</div>